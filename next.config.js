/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['vignette.wikia.nocookie.net', 'swift-sprint.testingnow.me'],
  }
}

module.exports = nextConfig
